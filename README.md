Лабораторна робота №6

ІО-35 Шевчук А.С. ФІОТ

Тема роботи

Налагодження модулів ядра Linux: виявлення та аналіз помилок виконання (kernel OOPS), створення інтерфейсів /proc, використання механізму seq_file для виведення діагностичної інформації та застосування інструментів налагодження ядра.

Мета роботи

Ознайомитися з методами налагодження модулів ядра Linux, навчитися викликати та аналізувати помилки ядра (kernel OOPS), створювати діагностичні інтерфейси у файловій системі /proc, працювати з API seq_file, застосовувати printk() і системні механізми збирання та перегляду налагоджувальної інформації.


debug/ — каталог модуля, який викликає OOPS

hello_debug.c — код модуля з помилкою (NULL-dereference)

Makefile — для побудови модуля hello_debug

proc/ — каталог для модуля з /proc інтерфейсом

hello_proc.c — код модуля, що створює запис у /proc

Makefile — для побудови hello_proc

seq/ — каталог для модуля, що використовує seq_file API

hello_seq.c — код модуля для seq_file

Makefile — для побудови hello_seq

README.md — файл опису проєкту / документації до лабораторної

screenshots/ — папка для зображень (скріншотів), де зберігатимуться результати тестів

(можливо інші файли, залежно від твоєї організації, але основні — як вище)
